# Quickstart

Run `conda run -n Zhijia python scripts/run_agentic_demo.py --config configs/demo.yaml`. The command needs only project dependencies and writes all artifacts below the configured external data root. Use `scripts/inspect_openpilot_log.py` only after setting `OPENPILOT_ROOT` and providing a local, explicit rlog/qlog path.
